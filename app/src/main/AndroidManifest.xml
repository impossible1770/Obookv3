<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.puls.composemultimoduletemplate">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--TODO: Use  usesCleartextTraffic only for api with open text traffic-->
    <application
        android:name="com.puls.composemultimoduletemplate.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_background"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_background"
        android:supportsRtl="true"
        android:launchMode="singleTask"
        android:theme="@style/Theme.Appiplay.Splash"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.puls.composemultimoduletemplate.presentation.ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>